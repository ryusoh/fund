- Use DOM insertion patterns (`document.body.appendChild(...)`) when testing empty states connected to `document.getElementById('runningAmountEmpty')` in chart renderers. Remember to clean up the nodes before the test finishes or during `afterEach`.
