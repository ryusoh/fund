"""Deterministic helper tools for the autonomous Jules routines.

These scripts move the "find the work" step out of agent judgement and into
tested, reusable code so routines select targets the same way every run.
See AGENTS.md and the persona files under .jules/.
"""
