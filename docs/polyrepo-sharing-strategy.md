# Polyrepo sharing strategy → moved

This doc has **moved to the foundation repo** and now lives there as the single
source of truth (keeping one copy is the strategy's own thesis — don't duplicate
across repos):

➡️ **[ryusoh/ryusoh · docs/polyrepo-sharing-strategy.md](https://github.com/ryusoh/ryusoh/blob/main/docs/polyrepo-sharing-strategy.md)**

It covers: why the `ryusoh/ryusoh` profile repo was chosen as the foundation (and
the decision record behind it), the four sharing "buckets" (CI / configs / brand
/ agent commands), the automated-PR machinery, and the performance analysis.

_Status: research, not yet implemented._ This pointer stays here so anyone in
`fund` can find the canonical doc.
